<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ========== РАЗРЕШЕНИЯ ========== -->

    <!-- Доступ к интернету — обязательно для VPN -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Проверка состояния сети (WiFi vs мобильный интернет) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>

    <!-- Для принудительного использования мобильной сети при тестах -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <!-- Foreground Service — чтобы VPN работал в фоне -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <!-- Запуск при перезагрузке телефона -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Показ уведомлений (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Работа в фоне без ограничений батареи -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:name=".VpnApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VlessVPN"
        android:usesCleartextTraffic="true">

        <!-- Главный экран приложения -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Экран настроек -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"/>

        <!-- VPN Сервис — сердце приложения -->
        <!-- android:permission гарантирует, что только система может его биндить -->
        <service
            android:name=".service.VpnTunnelService"
            android:exported="true"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="VPN tunnel service"/>
        </service>

        <!-- Фоновый сервис для загрузки и тестирования серверов -->
        <service
            android:name=".service.BackgroundMonitorService"
            android:exported="false"/>

        <!-- Receiver для запуска при перезагрузке -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

    
        <!-- Экран показа краша — открывается автоматически при падении -->
        <activity
            android:name=".util.CrashActivity"
            android:exported="false"
            android:process=":crash" />

        <!-- FileProvider — нужен для отправки лог файла через Telegram/email -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
