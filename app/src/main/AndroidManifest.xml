<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ========== РАЗРЕШЕНИЯ ==========
         ← ПОЧИЩЕНО: список ниже был продублирован (INTERNET и FOREGROUND_SERVICE
         встречались по 4 раза, SYSTEM_ALERT_WINDOW и REQUEST_INSTALL_PACKAGES —
         по 3, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS — 2). Дубли не ломают сборку
         (мёрджер манифеста и PackageManager сами схлопывают повторы), но затрудняют
         чтение и наводят на мысль, что часть блока копировалась вслепую при
         добавлении новых функций. Также убран android.permission.BIND_NETWORK —
         такого разрешения не существует в модели разрешений Android, это, судя по
         всему, опечатка/остаток от какой-то другой попытки; система его просто
         молча игнорирует, реального эффекта у него нет. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION"/>
    <queries>
        <!-- Все приложения -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>

        <!-- Приложения с лаунчером -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".VpnApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VlessVPN"
        android:usesCleartextTraffic="true">

        <!-- Главный экран приложения -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Экран настроек -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"/>

        <activity
            android:name=".ui.AppBlacklistActivity"
            android:label="Приложения вне VPN"
            android:parentActivityName=".ui.SettingsActivity"
            android:exported="false"/>

        <!-- VPN Сервис — сердце приложения -->
        <service
            android:name=".service.VpnTunnelService"
            android:exported="true"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="VPN tunnel service"/>
        </service>

        <service
            android:name=".service.BackgroundMonitorService"
            android:exported="false"/>

        <service
            android:name=".service.UpdateDownloadService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Receiver для запуска при перезагрузке -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.UpdateDownloadReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE"/>
            </intent-filter>
        </receiver>

        <!-- Экран показа краша — открывается автоматически при падении -->
        <activity
            android:name=".util.CrashActivity"
            android:exported="false"
            android:process=":crash" />

        <!-- FileProvider — нужен для отправки лог файла через Telegram/email
             ← ИСПРАВЛЕНО: этот <provider> был продублирован два раза подряд с
             одинаковым android:authorities="${applicationId}.provider". Мёрджер
             манифеста Android Gradle Plugin считает повторное объявление того же
             провайдера с тем же authorities конфликтом атрибутов и падает с
             ошибкой сборки ("Element provider#androidx.core.content.FileProvider
             ... is also present at ..."), либо (в зависимости от версии AGP)
             тихо оставляет только один экземпляр — в обоих случаях второй блок
             был чистым мусором. Оставлен один. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>

        <!-- AOD Overlay Service — отдельный класс, не влияет на основной код -->
        <service
            android:name=".service.AodOverlayService"
            android:exported="true"
            android:label="VlessVPN AOD"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/aod_accessibility_service"/>
        </service>

    </application>

</manifest>